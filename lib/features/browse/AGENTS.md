Keep Browse's DiscoveryGamesBloc and CollectionsBloc separate from Home's instances because both tabs remain alive. Refresh the whole hub and await its sections settling. Browse shows all collections; Home keeps its smaller cap.

Namespace cover Hero tags per Browse surface and forward the prefix through GoRouter extra to game details. Separate release rows need separate prefixes even within one tab. See `../../../docs/ui-reference.md` for the matching convention.
