Keep discovery state separate per DiscoveryType so one section cannot overwrite another; the view mode is shared. Preserve search debouncing, the 10,000 offset ceiling and existing results when loading another page fails. Cancel owned timers on BLoC disposal.

Keep cover Hero tags unique across simultaneously mounted surfaces and pass the source prefix to game details through GoRouter extra. Library actions use the shared LibraryBloc and library feature rather than duplicating mutations in game widgets.
